# SQL Server Credentials Example
# Filename: .env.p8054 (where 'p8054' matches Source/Target Host Name in Excel)

# Required fields
HOSTNAME=p8054.company.com
USERNAME=sql_user
PASSWORD=sql_password

# Optional fields (will use Excel values if not specified)
PORT=3085
DATABASE=BDM Archive

# Optional: SQL Server specific
# DRIVER={ODBC Driver 17 for SQL Server}
# For Windows Authentication, leave USERNAME and PASSWORD empty or omit them
